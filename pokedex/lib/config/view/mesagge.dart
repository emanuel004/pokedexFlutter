import 'package:flutter/material.dart';

class Mesagge extends StatelessWidget {
  const Mesagge({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('PASO A LA SIGUIENTE PANTALLA');
  }
}